# 付款界面与手动退款更新 — 2026-09-22

本包仅更新新社群相关功能。无需修改 `.env`，无需新增表或重跑支付迁移。复用现有新社群及共享支付账本；数据库账号仍需拥有这两个数据库的读写权限。

## 更新内容

- 付款主卡片按 `PAYMENT_DETAILS_DRAFT.md` 的用户文案显示：金额、网络、普通二维码、可复制的完整到账金额、收款地址、截止时间和求助提示。没有二维码中心图标。
- 「付款说明」仅点击者可见，保留钱包／交易所付款说明、实时费用示例和邀请信息；「呼叫管理员」保留暂停自动履约与频道保留功能。
- `/new_trade_review` 改用指令确认码，管理员不再点击处理确认按钮。确认码 5 分钟有效，绑定管理员、订单、决定和核对版本，一次使用。
- 新增手动退款登记及买家关闭确认：确认关闭即关闭；提出问题则保留；30 分钟无回应自动清理，后台检查可能延迟约一分钟。
- 重复登记、旧按钮、旧确认码不能导致重复退款或重置倒计时。重新通知关闭必须由管理员在保留状态下明确发起。
- 手动退款单独统计，不计入成交额。频道等待确认／保留期间继续保护证据，原论坛帖不会因一笔交易结束而删除。

## 部署

先备份当前代码和两个数据库。将更新包解压到 `/opt/discordbot`，保留包内 `modules/`、`utils/` 目录结构，不要只复制 `new_trading.py`。

```bash
sudo systemctl stop discordbot
# 将更新包中的文件覆盖到 /opt/discordbot 对应位置
cd /opt/discordbot
./venv/bin/python -m py_compile modules/new_trading.py modules/new_community.py modules/new_message_log.py modules/new_trade_stats.py utils/trade_review.py utils/trade_payment_ui.py utils/trade_card.py utils/shared_payments.py
sudo systemctl start discordbot
sudo journalctl -u discordbot -n 80 --no-pager
```

启动后等待新社群斜杠命令同步。旧付款卡片不会被批量改写；新生成的付款卡片采用新布局，旧「复制付款信息」按钮会打开新的付款说明。

## 管理员处理步骤

### 记录意见／处理正常匹配的异常入款

```text
/new_trade_review order_id:订单号 decision:记录意见 reason:核对意见
/new_trade_review order_id:订单号 decision:处理决定 reason:原因
/new_trade_confirm order_id:订单号 code:刚生成的确认码
```

「记录意见」直接保存，不启动关闭倒计时。其他决定须用确认码执行。金额错误不应选择继续履约、允许卖家收款或 bot 退款，只走手动退款。

「无到账关闭」表示管理员已经核实确实没有任何入款；bot 没匹配到账不等于没收到款，不能据此选择此项。

### 金额错误：管理员核实后手动退款

1. 用户呼叫管理员后，订单保持付款待核对，频道保留。管理员核实账户确实收到该用户的钱，核对用户身份、原入款及退款地址。不要凭公开的交易哈希或截图认定资金归属。
2. 管理员在币安账户手动退款；扣除实际网络费，不收服务费。不应同时操作该订单的其他放款／退款流程。bot 无法阻止人在币安界面重复转账。
3. 等币安记录显示退款完成后登记：

```text
/new_trade_manual_refund order_id:订单号 deposit_txid:原充值交易哈希 withdrawal_id:币安提现记录ID refund_address:与买家核对的退款地址 reason:处理原因
/new_trade_confirm order_id:订单号 code:刚生成的确认码
```

`withdrawal_id` 是币安提现历史中的记录 ID，不是链上交易哈希。系统从同一担保账户查询入款和提现历史，核对 USDT、BSC、地址、时间、完成状态，以及「退款实际到账＋网络费＝原实际入款」。金额由查询结果读取，不由管理员填写。

确认同时表示管理员已核对入款归属及退款地址。系统将原入款和退款流水唯一关联，拒绝已有提现记录（含处理中、失败、结果未知）的订单，拒绝同一流水用于另一订单。此入口仅记账，不会发起任何转账。

当前支持：一笔入款对应同一币安账户的一笔已完成外部 USDT-BSC 全额退款（仅扣网络费），账单在 89 天内。其他钱包转出、内部转账、部分退款、多笔合并、币种／网络不符或查询失败时，不能通过此入口自动结案，应保留频道单独核对。金额碰巧对应另一账单时也会停止登记。

### 手动退款后关闭频道

登记成功后，bot 单独 @ 双方并提供退款金额、费用、地址、交易哈希和关闭截止时间。只有买家可以点击：

- 「确认关闭」：同意清理交易频道，不触发转账。
- 「尚有问题／暂不关闭」：停止倒计时，保留频道并提醒管理员。
- 无回应：通知成功后 30 分钟自动关闭。无回应不作为退款到账证据。

管理员使用：

```text
/new_trade_channel order_id:订单号 action:暂停关闭 reason:原因
/new_trade_channel order_id:订单号 action:重新通知关闭 reason:问题已解决
```

暂停后的重新通知产生新的 30 分钟期限和按钮；旧按钮失效。重复执行重新通知不能重置正在进行的倒计时。重启后沿用已保存的截止时间。通知发送失败时不启动清理。

原有一般交易完成／取消／bot 退款仍按原约 5 分钟清理；手动退款不会混入该清理流程。频道等待确认或用户提出问题时，`/new_log_release` 不能绕过保留要求。

## 测试与依据

109 项自动测试已通过，覆盖付款精度、二维码顺序、费用查询失败、指令绑定与过期、重复提现记录拦截、入款及退款流水复用拦截、退款金额和状态校验、买家权限、旧按钮、拒绝关闭、通知失败、重复点击和倒计时恢复。所有支付接口均模拟，没有真实转账。

2026-09-23：临时数据目录改为不以点开头的 `test-mysql-review-*` 后，MySQL 8.0 成功启动，`tests/mysql_integration.py` 和 `tests/mysql_trade_review.py` 两套真实数据库集成测试均已通过。覆盖并发确认只结算一次、积分返还、入款／退款流水复用拦截、事务回滚、旧确认码失效，以及关闭倒计时持久化、拒绝关闭和到期清理。测试实例已停止；支付接口及 Discord 调用仍为模拟，没有真实转账，也未连接生产数据库。上线后仍需用测试订单核对 Discord 权限和实际通知效果。

账户历史接口和字段参考：[币安 Wallet Capital 官方文档](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/capital)。真实账户流水归属仍须管理员核实。
