# 新社群部署与检查

## 2026-09-12：外部 BSC 提现规则已由真实测试确认

用户提供的完成记录：提交 3.01 USDT，现货 USDT 总余额减少 3.01；提现历史 amount=3、transactionFee=0.01、transferType=0、status=6。因此该账户外部 BSC 提现采用金额内扣费，历史到账金额加网络费为账户扣款。接收钱包实际到账仍由用户独立核对。本次测试不代表其他网络或内部转账也已完成实测。

生产代码固定按 gross 提交，并明确 walletType=0（现货钱包）；手续费继续每次查询，不固定为 0.01。NEW_WITHDRAW_AMOUNT_MODE 已不读取，服务器可删除该变量，旧值不会影响新版计算。共享支付开关、逐单资金限额、重复提现保护和异常暂停仍生效。更新 modules/new_trading.py、utils/shared_payments.py、utils/payout_guard.py 后重启原服务即可，无需迁移数据库。以下早期文档中“待核实扣费语义”的说明已由本次实测更新。


## 2026-09-11：交易统计和资金限额

新增环境变量 `NEW_TRADE_STATS_CHANNEL_ID=新社群文字或公告频道ID`。留空关闭统计。bot 每分钟更新同一条英文面板，消息删除后重建，重启后恢复原消息。展示进行中各步骤、完成、取消、退款、争议和异常数量，以及按北京时间计算的今日/累计完成商品金额；不含服务费、网络费，不公开参与人、地址或账户余额。统计读取新数据库，删除交易频道不影响统计。

放款底层现在从数据库锁定本单订单、账单、到账流水及收款人确认记录后计算限额。正常卖家放款总额限定为商品金额，不能动用实际收取的服务费或账单尾数；退款上限为该单全部实际到账（含已付服务费和尾数），网络费从退款中扣除。提交前显示总额、预计网络费、预计到账及精度舍入差额；费用变化要求重新确认。积分抵扣部分不是现金退款，沿用原逻辑恢复积分。

同一订单的卖家放款和退款共用一个唯一提现记录。失败、超时或结果未知不会释放额度或重新提交。额度快照和提现预留在同一数据库事务内保存，外部提现在事务提交后发起。旧社群和未识别订单的自动放款在支付底层也被禁止。

提现成功记录还会核对币种、网络、地址、金额及费用。字段依据 [Binance 官方提现历史文档](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/capital#withdraw-history)。文档不足以替代该账户实际扣费语义核实；目前将历史金额加历史费用作为保守支出上界，如果历史金额已经含费，可能转人工核对。任何异常会保留原返回资料，将提现标为 `review`，写入持久化 `payment_settings.payout_freeze` 并告警，阻止后续新提现；已经提交的请求无法撤回。重启不会解除暂停，需先核对原提现结果与账本，再由维护者处理暂停记录，不能直接清空订单或提现记录来重试。

此次复用现有 settings、payment_settings、audit 和资金表，不需要新建表。共享数据库用户需要在同一个 MySQL 连接上读取新库订单和审计表。已有在途提现缺少新额度快照时转人工核对，因此更新前请先核对在途订单。37 项离线测试通过，新增资金规则尚未连接生产数据库或真实账户验证；本机制不能拦截手工提现或其他程序直接操作账户。

## 新社群动态语音频道

新增 `modules/new_voice.py`，随同一个服务运行，与旧社群组队语音隔离。先在新社群创建一个普通语音入口（例如「➕ Create Voice」），以及放置临时频道的分类，然后在服务器 `.env` 填写：

```env
NEW_VOICE_CREATE_CHANNEL_ID=入口语音频道ID
NEW_VOICE_CATEGORY_ID=临时语音分类ID
```

任一留空则不启用。用户加入入口后，bot 创建以用户昵称命名的语音频道并移动用户；频道继承目标分类的权限，不给创建者额外管理权限。创建者仍有频道时再次加入入口会返回原频道。每人新建间隔为 30 秒。

最后一名成员离开后删除频道；创建者离开但其他成员仍在时保留。新建频道有 10 秒保护期，每 30 秒补查空频道，重启后从数据库恢复已记录频道的清理。不删除入口或未登记的普通频道；管理员移出原分类的频道不自动删除。记录复用新数据库的 `settings` 表，无需新增表。

bot 在入口和目标分类需要查看频道、连接、移动成员、管理频道权限。用户在目标分类需要查看频道及连接权限。使用普通成员账号测试创建、两人加入后一人离开、全部离开、重启后清理。支付保持关闭时也能测试此功能。

此变更尚未连接 Discord、MySQL、Redis 或币安生产账户。不要把通过离线测试理解为已上线或已核实真实提现费用。

## 文件与配置

- `config.py` 通过 `config.NEW` 读取 `utils/new_config.py`；旧变量和旧手续费保持不变。
- `new-community.env.example` 是新增环境变量清单，不含旧配置或密钥。当前工作目录没有 `.env`，不要用示例覆盖服务器现有文件。
- `modules/new_community.py`：验证、语言身份组、邀请排名、通知。
- `modules/new_trading.py`：论坛入口、独立新订单、积分预留、争议决定、付款恢复。
- `modules/new_message_log.py`：最新正文缓存、编辑删除日志、容量及到期清理。
- `utils/shared_payments.py`：新旧账户级账单分配、到账认领、持久化提现请求。
- `modules/payment_recovery.py`：历史旧交易恢复实现，当前已停用且不加载。

论坛标签写法：`NEW_TRADE_BUY_TAG_MAP=论坛ID:buy标签ID,另一个论坛ID:标签ID`，sell 同理。
论坛 ID 和身份组列表使用英文逗号。通知频道不设环境变量，发布时由管理员选择。

新数据库、共享支付数据库、旧数据库必须是三个不同名字。沿用现有 MySQL 连接用户；该用户需要新库的表读写权限，安装阶段需要建库建表权限。共享积分预留需要访问旧库用户表。

## 上线顺序

1. 先备份旧代码、旧数据库、Redis 持久化数据及服务器 `.env`。确认恢复路径可用。
2. 使用与服务相同的 Python 环境，安装原 `requirements.txt`；保持 py-cord 2.4.1。
3. 填好新增变量，保持 `SHARED_PAYMENTS_ENABLED=false`。执行 `python setup_new_db.py`，只创建两个新库和新表。
4. 在新社群核对 bot 权限：查看频道、读历史、发消息、嵌入链接、发文件、管理频道/线程、获取邀请所需的管理服务器权限、管理身份组。启用成员和消息内容 intents。bot 最高身份组必须高于验证和语言身份组。
5. 共享支付切换要求旧社群所有交易和租赁先结束并核对资金。然后 `sudo systemctl stop discordbot`。
6. 执行 `python prepare_shared_payments.py`。有未结束旧订单会拒绝切换。该脚本保存旧订单金额禁用记录、阻止旧已结束订单再放款，并为旧 `transactions.status` 补充代码所用的 `confirmed_receipt` 枚举（保留其余成员）。请在数据库备份后执行。
7. 成功后把 `SHARED_PAYMENTS_ENABLED=true`。未执行切换脚本时，共享支付会拒绝工作，不会退回不安全的旧认领方式。
8. 外部 BSC 提现已实测确认为金额内扣费，新版无需配置 NEW_WITHDRAW_AMOUNT_MODE。网络费实时查询，提现结果继续核对。
9. `sudo systemctl start discordbot`。同一个服务同时运行两社群，重启时两边都暂时离线。

## 规则、语言与通知

`config/new_content.json` 存放可逐步替换的文案。`verify_body` 为空时不发布验证面板，填好真实规则后重启或等待下一次 10 分钟维护。`banners` 是 `频道ID: HTTPS图片链接` 的映射。

验证与语言频道分开。English 默认不发角色，额外语言最多一个；中文仅一个角色。验证不依赖语言选择。bot 重启后按钮仍通过固定 ID 处理。

`/new_notice`：管理员选择文字、公告或论坛频道，填写标题、正文、横幅，预览后确认；`message_id` 指定之前由该命令发布的消息 ID 可编辑；`copy_from` 指定旧通知 ID 可预填内容并重新发布到所选频道，两者不同时填写。论坛开启必选标签时需填写标签 ID。默认不 @everyone。

邀请成员以第一次被观察到的加入记录为准，同一用户不重复记；获得验证身份组才计有效。未知或有歧义来源不猜测归属，后续重新加入也不改归属。未在线期间的历史邀请来源无法恢复。排行榜前 10，每 10 分钟刷新，不发积分或排名身份组。

## 交易与人工处理

仅新社群保留右键 buy/sell，由新交易模块独立处理。旧社群交易、租赁及邀请排行榜已停用。
新订单默认 2 USDT 服务费，由买家承担，积分仅抵扣服务费，初始 0。商品价款最多两位小数。
一篇论坛贴可接待多买家，默认每人最多 5 个未结束订单；改标签刷新按钮，buy/sell 同选时停用入口。启动补齐当前活跃论坛贴，不自动重开已归档帖子。

买家需精确到账显示金额（包括识别尾数）；不能以转出金额代替到账金额。30 分钟超时进入人工核对，不删除资金订单。
卖家确认收货地址和预计费用后才放款；未知提现结果不重发。完成由提现历史确认，不以请求返回 ID 代替最终成功。

`/new_trade_review` 仅交易管理员可用，需原因和二次确认。支持记录意见、继续履约、允许卖家收款、退还买家、无到账关闭。放款/退款必须已有到账流水，资金领取者确认自己的地址；退款为已认领入款，退款网络费由领取方承担。正在提交或结果不明的提现不得重新发起另一笔。失败/未知提现需要核对原请求，不提供一键清除防重复记录。

已完成/取消/退款订单约 5 分钟后清理私人频道，双方可点击保留按钮请求管理员处理。论坛不因一笔订单结束而关闭。

## 消息容量

普通消息仅最新正文和附件名/链接，不下载原附件。论坛正文 30 天、私人交易结单后 7 天；编辑删除事件 180 天；消息轻量索引 180 天。活跃订单（包括来源论坛）或 hold 标记暂停清理。

容量指标为新消息表的估算活跃字节数（正文 UTF-8 字节、附件链接和每条 512 字节余量），不是整个 MySQL 物理文件上限：2 GB 触发批量清理，向 1.5 GB 回落；2.7 GB 告警并提前暂停新正文为证据预留空间；3 GB 拒绝继续增长并告警。按最旧非受保护记录清理；事件尽量留最近 30 天。每分钟最多一批 500 条，避免长时间阻塞数据库。

MySQL 页、索引及文件高水位会使物理占用不同于逻辑估算，不能用此机制替代主机磁盘监控或声称绝对 3 GB 磁盘上限。订单、资金与管理员审计表不纳入消息清理。若受保护记录占满容量，必须扩容或人工归档，不自动删除争议证据。附件链接可能失效；离线期间的完整编辑和删除历史无法保证恢复。

`hold` 用于争议或保留请求；管理员可使用 `/new_log_release`，填写频道 ID、原因并二次确认后解除。仍有未结束订单时拒绝解除。论坛有多笔订单时需逐一确认无争议。

备份应单独安排两份滚动副本，包含新库与共享账本，并测试恢复。不要把备份容量计入在线消息 3 GB；二进制日志和备份也必须设置保留期限。

## 验证与回退

离线：`python -m unittest discover -s tests -v`，`python -m compileall -q main.py config.py modules utils setup_new_db.py prepare_shared_payments.py`。

上线前在测试社群/测试数据库核对：普通用户不可跨社群操作、buy/sell 两入口、重复点击、各状态取消/争议、语言更换、验证重复点击、邀请未知来源、重启后按钮、删改和批量删除、容量压力及争议保护。资金测试应模拟失败、超时、重复回调、同时支付和迟到账；真实手续费行为仍需账户侧核实。

若尚未启用共享支付，可停服恢复旧代码与配置，保留新库。启用后已有新流水时，不能直接恢复旧支付代码或清空新账本/Redis，否则失去重复付款保护。应先停用新建交易、核对所有未结款项，再安排停服回退；共享账本必须保留。
# 2026-09-09：旧功能停用与新社群链接限制

交易横幅已接入：部署时将项目的整个 `png` 文件夹一并上传，保留六个 PNG 的原文件名（含空格和大小写）。不需要新增 `.env` 或修改数据库。程序按代码所在目录读取图片，不依赖服务的当前工作目录。

横幅仅在双方专属交易频道显示：建单成功显示 Initiate Trade；等待对方确认显示 Confirm Order；确认后至检查到账显示 Verify Payment；到账后等待卖家发货显示 Ship Item；发货后显示 Confirm Receipt；确认收货后及完成收款显示 Receive Payment。每条步骤消息的横幅在正文上方，历史步骤消息保留。论坛入口不加横幅；争议、取消和退款不展示正常收款横幅。缺失或无法读取图片时记录服务器错误并继续显示文字和操作按钮。bot 在交易频道需要 Attach Files 和 Embed Links 权限。

公共交易消息的最终语言确定为 English，不按买卖双方语言身份组切换或显示双语。现有正文暂不改写，待用户修改内容后统一翻译。

- 主程序不再加载旧交易、租赁、旧订单自动支付恢复模块；模块自身也拒绝加载。旧邀请统计、排行榜和排名身份组任务停止，旧排名命令被拦截。旧社群的其他功能（包括 Boost 处理）保留。
- 历史订单、余额、收付款账本、消息和已发放身份组不删除。停用后旧订单不会自动继续收付款；如果存在未结订单，需要按历史账本核对处理，不能将停用视为结清。共享支付迁移仍要求先处理旧未结订单。
- 新社群右键 buy/sell 已由新交易模块独立注册。更新后重启同一个 `discordbot` 服务，同步命令后生效；旧消息上的按钮可能仍显示，但旧处理程序不再运行。
- 在服务器 `.env` 增加 `NEW_LV3_ROLE_IDS=新社群LV3身份组ID`。多个可发链接的身份组用英文逗号分隔；身份组名称或排序更高不会自动算作 LV3。如需 LV4 等也可发链接，需同时保留 LV3 或将其 ID 加入列表。
- 该规则覆盖新社群文字频道和论坛回复的新消息及编辑，机器人消息除外；不沿用旧社群白名单。需要 bot 的查看频道、读取消息历史、管理消息权限，以及 Message Content Intent。通过删除消息实现，无法阻止消息短暂出现。识别 http/https/ftp、www 和 Discord 邀请链接，不是混淆网址识别器。
- 配置为空或全部无效时，新社群所有真人成员的可识别链接都会被删除，并在服务器日志报错。部署前务必填好。管理员也需要配置中的身份组。
- 删除前尝试保存交易范围内的证据，并在服务器日志及数据库审计中记录 bot 的删除操作；普通非交易频道不额外存储正文。
- 此次没有数据库结构变更。21 项离线测试及语法检查通过，未登录 Discord、未连接生产数据库或执行真实支付。

## 交易语言：当前决定

语言频道仍按选择发放身份组。公共交易消息最终统一 English，不做双语；本轮不改写现有交易文案，等用户确定内容后再翻译。商品描述和交易约定保持用户原文，金额、地址、订单号不翻译。

## 首次服务器测试

先测试不涉及收付款的功能：保持 `SHARED_PAYMENTS_ENABLED=false`，初始化新库后启动服务，检查通知、语言身份组、邀请排名、链接限制、论坛入口和删改日志。`verify_body` 为空时验证面板不会发布，测试验证需要先填写测试规则。

当前没有模拟交易模式。支付关闭时，右键和论坛入口会提示暂不开放交易，不会创建交易频道，所以该阶段不能走完整交易步骤或查看频道内步骤横幅。完整交易测试需要先停服完成共享账本迁移，使用已验证扣费方式的新版代码，再启用支付；该模式使用真实账户和实际资金。不要靠修改订单状态或填假到账流水来测试付款。

使用服务器现有 systemd 服务的 Python 环境，从项目目录执行安装/初始化命令。更新代码前停止服务并备份；不要复制 Windows 的 `.venv`、覆盖服务器 `.env`，也不要另开一个手动 bot 进程与服务同时运行。整个 `png` 目录需要上传。

启动后可用 `sudo systemctl status discordbot --no-pager` 和 `sudo journalctl -u discordbot -n 100 --no-pager` 查看结果。如需反馈故障，只提供相关错误堆栈，先移除 token、API 密钥等敏感字段。
