# 交易状态与超时恢复更新

本次直接修改项目文件，不生成更新压缩包。
服务器无需同步测试脚本；不新增批量取消 bot 指令，不修改 .env 或数据库结构。
服务器历史 payment_review 不会被本更新批量取消；此前的一次性清理仍需独立执行。

## 超时规则

- pending：从创建时间起 30 分钟。
- confirmed：从确认审计时间起 15 分钟，期限内需获取付款信息。
- 到期前 5 分钟通知双方；重启时已经错过提醒的，成功提醒后给予至少 5 分钟。
- 老 confirmed 缺少确认审计时间时，从首次检查起给予 15 分钟。
- 重复点击不重置时限。定时检查每 60 秒执行一次，实际通知/关闭有轮询延迟。
- 仅无账单、无入款、无提现、无积分预留的前期订单可自动 expired。
- 确认与超时使用订单行锁互斥，不能把已经进入下一步的订单覆盖成 expired。
- 原付款阶段的截止时间、5 分钟关闭提醒及申请人工核实规则保持不变。

## 恢复与保护

- 重启恢复沿用原账单地址、精确金额和期限；过期账单进入核对/超时流程，不重新生成账单。
- 生成账单持续异常 15 分钟后转 payment_review 并保留积分预留，由管理员核对处理。
- 新创建频道的 topic 保存完整订单号，可恢复创建频道后、写入关联前中断的建单。
- 无频道且无资金记录的前期订单可结束；付款频道确实不存在时转人工核对，不推断未付款。
- Discord Forbidden/网络异常不代表频道已删除，不触发无频道结案。
- 改 paid、消耗预留积分和审计在同一事务；失败回滚，重复处理不会再次消耗积分。
- 资金检查先执行，频道删除失败独立记录并重试，不阻断其他订单。
- 普通频道清理与“保留频道”共用锁，重新检查最新状态和保留标记。
- 订单记录作为持久通知待办，分别保存卡片与提醒送达状态；失败后补发最新状态。
- 发送成功但响应/送达标记丢失时，可能重复通知；补发不会再次执行付款或提现。
- 已到账、争议和提现中的订单不会按前期时限自动取消。

## 部署

1. `sudo systemctl stop discordbot`
2. 备份当前代码与数据库，将以下文件按目录同步到 `/opt/discordbot`：
   - `modules/new_trading.py`
   - `modules/new_message_log.py`
   - `modules/new_trade_stats.py`
   - `utils/trade_states.py`（新增）
   - `utils/trade_idle.py`（新增）
   本次基于已包含 `utils/trade_timeout.py` 的上一版代码，服务器需先具备该版本。
3. 不需要执行数据库初始化或切换支付账本脚本；不需要新增环境变量。
4. `sudo systemctl start discordbot`
5. `sudo journalctl -u discordbot -n 100 --no-pager`

启动后旧的 pending/confirmed 也会按上述规则检查。有频道的过期旧订单会先收到至少 5 分钟提醒。
若生产环境还有另一份同 bot 进程，应先停止；频道互斥锁按单进程设计。

## 验证

本地完整单元测试 123 项通过；独立临时 MySQL 的基础账本、人工退款、超时与迟到账、状态竞争和积分回滚测试通过。
外部支付和 Discord 调用使用替身，未执行真实转账，未连接生产数据库。
