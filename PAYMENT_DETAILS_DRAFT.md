# 付款详情与管理员结案草稿

状态：已按确认方案实现于本地代码，待部署测试。金额、地址及时间均读取实际订单。

`{{变量}}` 为订单数据占位符；二维码位置说明不作为实际消息发送。

## 一、付款主卡片

保留顶部「Verify Payment」步骤横幅。主卡片只展示付款必要信息，详细说明通过按钮单独查看。

---

## {{到账金额}} USDT

**网络：BSC / BEP20 · USDT**

〔收款地址二维码，无中心图标〕

**链上必须实际到账**  
```
  {{到账金额}}
```
**收款地址**
```tex
{{收款地址}}
```
**付款截止：** {{截止时间}}（{{剩余时间}}）

付错金额或到账未识别？请呼叫管理员，**不要重复付款或自行补差额**。

-# 订单 {{订单号}}

---

卡片下方用户按钮：
- 💡 付款说明
- 🆘 呼叫管理员

说明：“付款说明”按钮仅向点击者展示详细规则，不占用交易频道正文。

## 二、单独发送的双方提醒

生成付款信息后，由 bot 另发普通消息，触发双方的 @ 提及：

> @买家 请按上方金额付款。@卖家 请等待系统确认到账，暂勿发货或代付。

不在付款主卡片中重复放置这段角色说明。其他交易步骤继续单独提醒相应买家或卖家。

## 三、点击「付款说明」后的内容

仅点击者可见，避免占用交易频道正文空间。

### 钱包转账

发送 **{{到账金额}} USDT**，普通 BSC 钱包的网络费另用 BNB 支付。请核对网络和金额。

### 币安或其他交易所提现

最终的**实际到账金额**必须为 **{{到账金额}} USDT**。

仅当币安从填写金额扣除 **{{币安当前费用}} USDT** 时，填写 **{{到账金额加费用}} USDT**。

若页面显示的到账金额已正确，**不要再次加手续费**。其他平台按其实际费用核对。

费用查询失败时，不展示加费示例，只提示核对最终到账金额。金额与费用均使用实时数据，不写死 0.01。

### 为什么不能省略小数？

小数尾数用于识别订单。请勿只付整数、商品价或保留两位小数；金额不一致可能无法自动识别。

### 已付错或未识别

点击「呼叫管理员」，在交易频道提供实际金额、交易哈希和付款截图。请勿重复付款或自行补差额。

付款金额错误时，只能由管理员核实实际收到款项后手动退款，不通过 bot 自动补单、继续履约或发起退款。退款不收服务费；如有网络费，从实际收到的款项中扣除，并告知用户退款金额和网络费。

### 邀请信息

将原有邀请信息移到说明末尾，不占用付款主卡片。链接与邀请码保持用户提供的内容：

- 邀请链接：https://www.bsmkweb.cc/register?ref=1024490102
- 邀请码：`1024490102`

此处仅保留用户提供的信息，不代表已验证该域名归属。

## 四、管理员介入后的频道关闭

### 已确认的交互方式

**管理员使用指令处理和结案，不增加管理员结案按钮。** 用户仍可通过「呼叫管理员」按钮申请协助。

用户求助后，订单暂停自动履约并保留频道。管理员只是查看或记录意见，不应自动关闭频道。

### 常规订单处理规则

- `/new_trade_review` 的「记录意见」仅保存意见，不结束订单。
- 「无到账关闭」将订单取消并解除该交易频道的保留，随后约 5 分钟清理。
- 「退还买家」只授权退款；还需买家确认退款地址，等系统确认退款完成后才清理频道。
- `/new_trade_review` 生成 5 分钟有效的一次性确认码，通过 `/new_trade_confirm` 执行决定。
- `/new_trade_manual_refund` 核对已发生的手动退款并生成确认码；此指令本身不转账。
- 系统没有匹配到订单入款，不等于用户没有付款，特别是金额错误的情况。

### 指令流程

沿用 `/new_trade_review` 的核对入口，并统一采用两步指令确认：

1. 管理员提交订单号、处理决定、原因及必要凭证，bot 仅显示处理预览并返回短时确认码。
2. 管理员再次通过确认指令提交订单号与确认码，bot 重新检查订单和资金状态后执行。确认码绑定管理员、订单和本次决定，只能使用一次；订单状态变化则失效。

指令名称与参数：

```text
/new_trade_review order_id:订单号 decision:处理决定 reason:原因
/new_trade_confirm order_id:订单号 code:确认码
```

「记录意见」可以直接记录，不需要第二步。其他会改变订单状态的决定通过第二条指令确认，全程不使用管理员确认按钮。

### 不同结案情况

**确认确实未付款**

管理员核对后选择「无到账关闭」。不能只因 bot 没匹配到金额就认定未付款。确认后取消订单，通知双方，约 5 分钟后关闭频道。

**已经通过 bot 退款**

沿用系统退款状态。只有确认退款完成，才进入频道关闭流程；退款处理中或结果未知时不能强制结案。

**管理员已经手动退款**

金额错误的订单只走此流程：管理员核实担保账户的真实入款、确认付款归属和退款地址后，在外部手动退回。核实前保留频道，不开放自动履约或 bot 退款。退款以实际收到的金额为基础，退还服务费，仅扣除实际网络费，并向用户说明。

使用 `/new_trade_manual_refund`，提供订单号、原入款交易哈希、币安提现记录 ID、与买家核对的退款地址及原因。金额和费用从账户历史读取，记录原入款、退款总支出、实际到账金额、网络费及处理原因。再次提交 `/new_trade_confirm` 完成登记。

系统需核对流水关联和金额关系，确认没有在途或重复退款风险；记录为手动退款结案，而不是伪造 bot 自动退款成功。此操作只登记已发生的退款，**不再发起转账**。

付款金额不一致的订单，需要先可靠关联真实入款。不能直接改成「无到账关闭」，也不能仅凭截图把资金记为已退。

### 手动退款后的关闭确认

管理员使用指令登记并核实退款完成后，bot 单独 @ 买家和卖家，展示退款到账金额、网络费、退款凭证和关闭截止时间。由退款接收人（通常为买家）确认是否可以关闭频道，不要求卖家再次同意退款。

- 用户点击「确认关闭」：表示同意关闭频道，bot 提示后关闭该交易频道；不触发任何转账。
- 用户点击「尚有问题／暂不关闭」：取消自动关闭倒计时，保留频道并通知管理员。处理完毕后，由管理员通过指令重新发起关闭确认。
- 用户既未确认也未拒绝：从关闭通知成功发送起，30 分钟后自动关闭频道。
- 未收到退款的用户可选择「尚有问题／暂不关闭」。不回应仅作为频道清理条件，不能作为收到退款的证据，也不能替代退款核实。

管理员仍全程使用指令；以上两个按钮仅供退款接收人使用。卖家如提出争议，管理员可通过指令暂停关闭。

关闭期限、用户决定和通知消息应持久保存。重复点击或重复提交同一结案指令不得重复退款、重复发起倒计时或重置截止时间；重启后继续原倒计时。通知发送失败不能启动倒计时。删除频道前重新检查用户决定和管理员保留状态，已申请保留则不得删除。

**仍待核实或退款结果未知**

保留频道，不关闭，不开放重复放款。管理员可通过指令追加核对意见。

### 关闭范围与记录

- 手动退款结案按上述用户确认／无回应 30 分钟关闭的流程处理，不进入原有约 5 分钟的终态清理流程。其他结案类型暂保持原规则。
- 只清理双方交易频道，不删除接待多个买家的原论坛交易帖。
- 保留订单、入款、退款和管理员操作记录。
- 原论坛的证据保留，需要检查是否仍有关联的未结订单，不能随一笔订单结案而直接解除所有保留。

## 五、实施范围与操作说明

- 已实现付款说明按钮、管理员指令确认和手动退款后的关闭流程；部署步骤见 `DEPLOY_PAYMENT_REVIEW_20260922.md`。
- 手动退款核验当前支持同一币安担保账户中已完成的外部 USDT-BSC 退款，一笔入款对应一笔全额退款（只扣网络费）。不支持凭截图、其他钱包转出、内部转账、部分退款或多笔合并后自动结案；这些情况保持频道等待单独核对。
- 账单超过 89 天、流水查询失败、金额与其他账单冲突或已有任何提现记录时，不自动登记结案。
- `/new_trade_channel` 可暂停关闭，或在保留状态下重新通知关闭。重新通知才建立新的 30 分钟期限；一般重复操作不重置期限。
- 自动清理每分钟检查一次，30 分钟到期后可能有约一分钟的检查延迟。重启沿用原截止时间。
- 30 分钟确认期间及用户提出问题后，证据不会因容量清理而提前删除。已发出的旧付款消息不会被批量改写，新生成的付款卡片采用新布局。

