# 最低商品金额更新 — 2026-09-24

商品价格最低 5.01 USDT，最多两位小数。购买／出售共用入口；低于下限时提示“商品金额不能低于 5.01 USDT。”已创建但尚未付款的低价订单也不能继续生成付款信息，须取消后重新发起。

保留实时提现条件核验。若平台最低金额或费用变化，简短提示当前最低所需金额；已付款订单提示联系管理员，不解释网络费计算过程，不改变已付款订单金额。

每位用户默认最多同时进行 5 笔未结束交易，买家和卖家参与的订单合计计算，双方分别检查。付款待核对、争议及退款处理中也占用名额。完成、取消、已退款、手动退款已登记及测试结清的订单不占用名额。

上限由服务器 `.env` 的 `NEW_MAX_ACTIVE_TRADES` 控制，未设置则为 5。当前工作区没有 `.env`，因此无法确认服务器是否另行覆盖。此次未改变上限配置，只将实际生效数字加入报错提示。

本包包含 2026-09-22 更新包及本次两个代码文件更新。停服后按目录覆盖 `/opt/discordbot`，再启动 `discordbot`。无需新增配置或迁移数据库。不要用较旧更新包覆盖本包。

本次 23 项相关测试通过；增加的 3.01／5.00 拒绝用例和 5.01 接受边界检查也已通过。没有真实转账。
