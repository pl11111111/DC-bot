# 交易步骤提醒与付款求助

覆盖 `modules/new_trading.py`、`modules/new_community.py`、`utils/trade_payment_ui.py` 后重启 `discordbot`。无需修改数据库结构或新增环境变量。

- 每次交易卡片进入新的步骤，另发消息 @ 对应买家/卖家。同一步骤重新渲染不重复提醒；建单继续单独提醒双方。
- 展示付款信息时明确：买家付款，卖家只查看并等待系统确认到账，不应代付或提前发货。
- 付款卡片强调保留全部小数位，先复制金额并核对实际到账，不能四舍五入、只付整数或仅付商品价。
- 付款中和付款待核对消息新增“付款有问题／呼叫管理员”。只允许交易双方操作。操作将订单转为待核对，保留交易频道及关联论坛证据，暂停自动履约；同时提醒 `NEW_TRADE_ADMIN_ROLE_IDS` 中的身份组。
- 提醒优先发到 `NEW_ALERT_CHANNEL_ID`；未配置有效提醒频道时发到该交易频道。连续 5 分钟内重复求助不重复通知管理员。是否产生推送仍受 Discord 身份组提及权限及用户通知设置影响。
- 求助按钮不会发起退款，不会自动认领金额不一致的入款。管理员需核对真实到账流水、币种、网络、交易哈希及是否已退，确认后再手动处理并保存凭证。用户不要自行补差额或重复支付。
- 新生成的卡片使用新文案和按钮，历史卡片不会因文件上传自动变化。

26 项针对性离线测试通过，覆盖步骤提醒对象、重复求助、无权限请求、频道保留、付款金额和原有付款安全检查。没有执行生产转账或退款。
