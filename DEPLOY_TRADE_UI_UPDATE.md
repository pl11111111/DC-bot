# 交易界面、二维码与测试结清更新

将更新包内的 `modules`、`utils` 文件按原目录覆盖到 `/opt/discordbot`，不要放入新的同名项目子目录。此次无需修改 `.env` 或数据库结构。二维码使用项目原有的 `qrcode` 和 Pillow 依赖。

```bash
cd /opt/discordbot
./venv/bin/python -c "import qrcode; from PIL import Image; print('QR dependencies OK')"
sudo systemctl restart discordbot
```

若二维码依赖缺失，先按项目 `requirements.txt` 安装依赖再重启。旧消息不会自动转换，新订单或后续步骤使用新布局。

## 本人测试订单结清

管理员在新社群使用 `/new_trade_close_test`，填写：

- `order_id`：`540de081bedd4ea3a1584efbd494dae6`
- `reason`：`本人测试资金，全部到账款留存担保账户，测试结清`

核对机器人显示的订单和实际到账金额后，点击确认按钮。此操作不会转账或退款；资金留存在担保账户，数据库保存管理员声明及入款关联。订单状态为 `test_closed`，不计入正常成交额，领取按钮失效，约 5 分钟后清理频道。

只有已确认收货、有匹配到账记录且从未生成提现记录的订单可使用此入口。即使提现记录为失败或未知，也会拒绝测试结清。不要用于其他人的货款。

## 新布局

- 普通步骤保留一张当前步骤横幅；价格与托管费、订单合计与卖家货款、买家与卖家成组显示。
- 付款步骤突出账单实际到账金额，显示 USDT-BEP20 网络、条件式币安手续费提示、截止时间、收款地址与二维码。
- 二维码只编码本订单的收款地址，不自动指定币种、网络或金额；用户仍需核对。二维码本地生成，不调用外部二维码服务。
- “复制付款信息”按钮仅向点击者显示原账单的可复制金额和地址，不生成新账单，不直接写入剪贴板；过期或已收到款的账单拒绝继续提供付款信息。
- 商品价格下限统一为 3.01 USDT（表单、提交及获取付款信息一致），托管费另外计算。创建订单、获取付款信息前仍检查实时提现规则，若网络费用上涨，则按实时要求提示。

## 验证范围

71 项离线测试通过，覆盖卡片结构、二维码内容、金额精度、最低金额校验、超时保留及关闭、测试结清与已有提现互斥等。没有连接生产数据库或发起转账；真实 Discord 展示需部署后查看。
